# SeaAir Mobile App API Environment Variables

# Server Configuration
PORT=3000

# AWS Cognito Configuration (Required for JWT Authentication)
# Get these values from your AWS Cognito User Pool
COGNITO_USER_POOL_ID=us-east-1_xxxxxxxxx
COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
AWS_REGION=us-east-1

# AWS Credentials (Required for DynamoDB access)
# Get these from AWS IAM or use IAM roles for EC2/ECS/Lambda
# For local development, you can set these values
# For production, use IAM roles instead of hardcoding credentials
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# Logging Level (optional)
# LOG_LEVEL=info
